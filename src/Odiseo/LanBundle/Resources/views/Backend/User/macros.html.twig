{% macro list(users) %}

{% import 'SyliusResourceBundle:Macros:buttons.html.twig' as buttons %}
{% import 'GeckoBackendBundle:Macros:alerts.html.twig' as alerts %}
{% import 'GeckoBackendBundle:Macros:misc.html.twig' as misc %}

{% if users|length > 0 %}
<div class="box">
	<div class="box-body table-responsive no-padding">
		<table class="table table-hover"><tbody>
			<tr>
				<th>ID</th>
				<th>Nombre y Apellido</th>
				<th>Nombre Usuario</th>
				<th>Email</th>
				<th></th>
			</tr>
			{% for user in users %}
	        <tr>
				<td>{{ user.id }}</td>
				<td>{{ user.fullname }}</td>
				<td>{{ user.username }}</td>
				<td>{{ user.email }}</td>
				<td>
					<div class="pull-right">
	                    {{ buttons.edit(path('odiseo_lan_user_update', {'id': user.id})) }}
	                    {{ buttons.delete(path('odiseo_lan_user_delete', {'id': user.id})) }}
	                </div>
				</td>
			</tr>
	        {% endfor %}
		</tbody></table>
	</div>
</div>
{% else %}
{{ alerts.info('gecko.backend.no_results'|trans) }}
{% endif %}

{% endmacro %}